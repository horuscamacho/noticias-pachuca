import { Redirect } from "expo-router";
import React from "react";

/**
 * Demo screen showcasing all ThemedText variants
 */
const AppScreen = () => {
  return <Redirect href={"/(onboard)"} />;
};

export default AppScreen;
